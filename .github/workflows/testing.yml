name: testing
on: [push, workflow_dispatch ]
jobs:
    my_job:
        runs-on: ubuntu-latest
        steps:
            - name: acion
              uses: actions/checkout@v4
            - name: ghio
              run: npm install
            - name: jfdo
              run: npm run test

