name: test context
on:
    push:
        branches:
            - main
jobs:
    jo_1:
        runs-on: ubuntu-latest
        steps:
            - name: context
              run: echo "${{ toJson(github.event) }}"